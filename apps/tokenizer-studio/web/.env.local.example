# Optional explicit API base URL.
# If omitted: dev defaults to http://127.0.0.1:8000/api/v1 and production defaults to /api/v1.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000/api/v1

# Optional runtime header token used when backend enforces TOKENIZER_STUDIO_RUNTIME_TOKEN.
# NEXT_PUBLIC_RUNTIME_TOKEN=
