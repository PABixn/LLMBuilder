# Core runtime mode
TOKENIZER_STUDIO_HOST=127.0.0.1
TOKENIZER_STUDIO_PORT=8000
TOKENIZER_STUDIO_SERVE_WEB=1

# Optional root for app data.
# If omitted, defaults to OS-specific app data:
# - macOS: ~/Library/Application Support/TokenizerStudio
# - Windows: %AppData%\TokenizerStudio
# - Linux: ~/.local/share/TokenizerStudio
# TOKENIZER_STUDIO_DATA_DIR=

# Optional cache root (defaults to OS cache dir for TokenizerStudio).
# TOKENIZER_STUDIO_CACHE_DIR=

# Storage paths (relative paths resolve from TOKENIZER_STUDIO_DATA_DIR).
TOKENIZER_STUDIO_OUTPUT_DIR=artifacts/tokenizers
TOKENIZER_STUDIO_UPLOAD_DIR=uploads
TOKENIZER_STUDIO_DB_PATH=db/tokenizer_studio.db
TOKENIZER_STUDIO_LOG_DIR=logs

# Hugging Face cache paths (relative paths resolve from TOKENIZER_STUDIO_DATA_DIR).
TOKENIZER_STUDIO_HF_HOME=cache/huggingface
TOKENIZER_STUDIO_HF_DATASETS_CACHE=cache/huggingface/datasets

# Web build output served by the API in desktop/prod mode.
# Relative paths resolve from the api directory.
TOKENIZER_STUDIO_WEB_DIST_DIR=web-dist

# Optional API runtime header token (desktop shell can inject this).
# TOKENIZER_STUDIO_RUNTIME_TOKEN=

# Optional CORS overrides.
# TOKENIZER_STUDIO_CORS_ORIGINS=http://127.0.0.1:3000,http://localhost:3000
# TOKENIZER_STUDIO_CORS_ORIGIN_REGEX=http://(localhost|127\.0\.0\.1)(:\d+)?

# Optional full SQLAlchemy URL override (takes precedence over TOKENIZER_STUDIO_DB_PATH).
# TOKENIZER_STUDIO_DATABASE_URL=sqlite:////absolute/path/to/tokenizer_studio.db

# Worker and logging controls.
TOKENIZER_STUDIO_MAX_WORKERS=1
# TOKENIZER_STUDIO_ACCESS_LOG=0
# TOKENIZER_STUDIO_LOG_LEVEL=info
