name: Training Image

on:
  push:
    branches: [main]
    paths:
      - "docker/training/**"
      - "apps/llm-studio/remote_agent/**"
      - "apps/llm-studio/api/requirements.txt"
      - "model/**"
      - "tokenizer/**"
      - "training/**"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/metadata-action@v5
        id: meta
        with:
          images: ghcr.io/${{ github.repository_owner }}/llm-builder-training
          tags: |
            type=sha
            type=raw,value=latest,enable={{is_default_branch}}
      - name: Capture build timestamp
        id: build_time
        run: echo "value=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_OUTPUT"
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/training/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            LLM_STUDIO_IMAGE_REVISION=${{ github.sha }}
            LLM_STUDIO_IMAGE_BUILT_AT=${{ steps.build_time.outputs.value }}
